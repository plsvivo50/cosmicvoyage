# CosmicVoyage 代码异味扫描报告

**日期**: 2026-05-16（Day 2 — 代码异味大扫除）  
**扫描范围**: 核心模块（飞行系统 + 维度切换 + 数据包系统）  
**目标文件**: 8 个 Java 源文件，共 1,387 行  
**扫描维度**: 魔法数字 / 硬编码字符串 / 空 catch / 资源泄漏 / 重复代码 / 过长方法 / 跨层调用

---

## 一、扫描统计速览

| 指标 | 数量 |
|:---|:---:|
| 扫描文件数 | 8 |
| 总代码行 | 1,387 |
| **P0 异味项** | **7 项** |
| **P1 异味项** | **11 项** |
| **P2 异味项** | **6 项** |
| `System.out.println` 硬编码日志 | 4 处 |
| `catch(Exception)` 全吞模式 | 1 处 |
| 跨层依赖调用 | 5 处 |
| Public 字段直接访问 | 17 处 |
| 重复代码块（Earth vs Moon）| 74-77% 行重复 |

---

## 二、P0 级异味（崩溃 / 数据丢失 / 安全漏洞风险）

> 第二阶段开始前**必须修复**。不修复将直接导致第二阶段开发受阻或运行时故障。

---

### P0-1: `ShipEntity.shipVelocity` / `hasImpulse` Public 字段暴露

| 属性 | 内容 |
|:---|:---|
| **位置** | `entity/ShipEntity.java` L27, L26 + `network/CosmicVoyagePacketHandler.java` 多处 |
| **严重性** | **高 — 已在 5月11日引发生产事故** |
| **问题描述** | `shipVelocity` 和 `hasImpulse` 声明为 `public`，`CosmicVoyagePacketHandler` 直接读写赋值（L78-80, L182-184, L216-218, L269-271）。Network 层直接操作 Entity 层字段，破坏封装。 |
| **历史事故** | 5月11日 21:00，`handleShipSync` 中移除 `hasImpulse = true` 导致飞船冻结功能失效 → 超前/被甩下/解除骑乘。修改 network 层代码意外破坏了 entity 层行为。 |
| **修复方案** | 1. `shipVelocity` → `private`，添加 `getShipVelocity()` / `setShipVelocity()` / `applySyncPacket(x,y,z,yaw,pitch,vx,vy,vz)` 方法<br>2. `hasImpulse` → `private`，network 层不再直接触碰<br>3. 所有同步逻辑集中到 `ShipEntity.applySyncPacket()`，network 层只调用方法 |
| **关联架构问题** | R3（架构报告） |

---

### P0-2: `SpaceData` 单锚点设计 — 第二阶段阻塞项

| 属性 | 内容 |
|:---|:---|
| **位置** | `data/SpaceData.java` 全文件 |
| **严重性** | **高 — 返航系统无法闭环** |
| **问题描述** | 仅存储地球锚点（`anchorX/Y/Z` + `hasAnchor`），无月球锚点存储。`handleLaunchToSpace` 调用 `setAnchor()` 保存地球位置，但月球起飞位置无处存储。 |
| **影响** | 月球起飞后无法返回对应太空坐标；`CvSpaceCommand` 的 `/cvspace return` 只能回地球，不能从月球返航太空。5月11日返航系统因此无法完整实现。 |
| **修复方案** | 扩展双锚点系统：<br>1. 添加 `moonAnchorX/Y/Z` + `hasMoonAnchor` 字段<br>2. 添加 `setMoonAnchor()` / `getMoonAnchor()` / `hasMoonAnchor()` API<br>3. NBT 序列化/反序列化同步更新<br>4. `handleMoonTransition` 保存月球锚点，`handleLaunchToSpace` 区分地球/月球锚点 |
| **关联架构问题** | R2（架构报告） |

---

### P0-3: `ModDimensionEffects` 反射注册 + 双注册冲突风险

| 属性 | 内容 |
|:---|:---|
| **位置** | `client/ModDimensionEffects.java` L25-53 |
| **严重性** | **高 — 启动时行为不确定** |
| **问题描述** | 通过反射 (`getDeclaredField("EFFECTS")` + `setAccessible(true)`) 直接修改 Mojang 私有 Map。`CosmicVoyage.ClientSetup` 已通过 `RegisterDimensionSpecialEffectsEvent` 注册了相同效果。两个注册路径互相覆盖，行为取决于事件时序，可能在不同 Forge 版本表现不同。 |
| **风险** | Java 模块系统可能阻止 `setAccessible`；Mojang 随时可能更改字段名/类型导致注册失败。 |
| **修复方案** | **删除 `ModDimensionEffects.java`**。只保留 `RegisterDimensionSpecialEffectsEvent` 事件注册方案。反射方案是已被替代的旧代码。 |
| **关联架构问题** | R1（架构报告） |

---

### P0-4: `catch(Exception e)` 全吞 — 维度效果注册失败静默

| 属性 | 内容 |
|:---|:---|
| **位置** | `client/ModDimensionEffects.java` L48-51 |
| **严重性** | **中-高** |
| **问题描述** | 捕获 `Exception` 超类，只打印日志不抛出，不中断启动流程。如果维度效果注册失败，玩家进入太空/月球时天空渲染异常，但无明确错误提示。 |
| **修复方案** | 1. 捕获具体异常 (`NoSuchFieldException`, `IllegalAccessException`)<br>2. 使用 `CosmicVoyage.LOGGER.error()` 替代 `System.err.println`<br>3. 注册失败时抛运行时异常终止启动，** fail-fast ** |
| **关联** | P0-3 删除本文件后此问题一并消除 |

---

### P0-5: `CosmicVoyagePacketHandler` 三合一文件 — 单一职责严重违规

| 属性 | 内容 |
|:---|:---|
| **位置** | `network/CosmicVoyagePacketHandler.java` 全文件 291 行 |
| **严重性** | **高 — 第二阶段新增 Packet 将成瓶颈** |
| **问题描述** | 一个文件包含：<br>• 3 个 Packet 类定义（`ShipSyncPacket`, `MoonTransitionPacket`, `LaunchToSpacePacket`）<br>• 3 个 Handler 方法（`handleShipSync`, `handleMoonTransition`, `handleLaunchToSpace`）<br>• 网络通道注册逻辑<br>第二阶段新增行星将需要更多 Packet，此文件将膨胀到 500+ 行。 |
| **修复方案** | 拆分为独立文件：<br>1. `network/packet/ShipSyncPacket.java`<br>2. `network/packet/MoonTransitionPacket.java`<br>3. `network/packet/LaunchToSpacePacket.java`<br>4. `network/PacketHandler.java`（只负责注册+分发）<br>每个 Packet 的 encode/decode/handle 内聚在各自文件中。 |
| **关联架构问题** | R6（架构报告） |

---

### P0-6: `handleLaunchToSpace` 硬编码传送坐标

| 属性 | 内容 |
|:---|:---|
| **位置** | `network/CosmicVoyagePacketHandler.java` L261 |
| **严重性** | **中 — 历史已引发循环传送 Bug** |
| **问题描述** | 传送目标 `(0, 200, -600)` 是 5月11日修复循环传送 Bug 的硬编码魔数。注释说明 `-600` 确保距离地球 > 500（`TRIGGER_LAND_EARTH`），但如果着陆触发距离或地球位置改变，可能再次出现死循环。 |
| **历史事故** | 5月11日 15:00，`(0, 200, 0)` 距离地球 = 200 < 500，进入太空立刻触发返回，形成死循环。 |
| **修复方案** | 使用 `SpaceConstants` 推导：`SPACE_ENTRY_POS = new Vec3(0, 200, -(EARTH_RADIUS + TRIGGER_LAND_EARTH + 100))`，确保数学上不可能触发着陆。 |
| **关联架构问题** | R4（架构报告） |

---

### P0-7: `SpaceState` 内存泄漏 — 无退出清理机制

| 属性 | 内容 |
|:---|:---|
| **位置** | `space/SpaceState.java` L10 |
| **严重性** | **中 — 长期运行风险** |
| **问题描述** | `Set<UUID> IN_SPACE` 只在 `enterSpace()` 时添加，`exitSpace()` 只在正常流程调用。玩家崩溃、断线、强制关游戏时 `exitSpace()` 不被调用，UUID 永久残留。长期运行后 Set 无限增长。 |
| **修复方案** | 1. 订阅 `PlayerLoggedOutEvent` 清理下线玩家 UUID<br>2. 或改用 `WeakReference` + 定期扫描过期条目 |
| **关联架构问题** | R9（架构报告） |

---

## 三、P1 级异味（可维护性影响）

> 建议第二阶段初期修复。不影响功能正确性，但会增加维护成本和 Bug 引入概率。

---

### P1-1: `EarthRenderer` + `MoonRenderer` 74-77% 代码重复

| 属性 | 内容 |
|:---|:---|
| **位置** | `client/EarthRenderer.java` (311行) + `client/MoonRenderer.java` (258行) |
| **重复方法** | `renderWireSphere()` (57行, 差异 < 1%)、`renderEquatorLine()` (25行)、`renderLODFar/Mid/Near()`、`renderInsideIndicator()`、`onRenderLevelStage()` |
| **重复比例** | Earth: 229/311 行 (74%) 为可复用模式代码；Moon: 198/258 行 (77%) |
| **风险** | 修改通用渲染逻辑（如线框球体细分度、颜色计算）需改两个文件，极易遗漏。 |
| **修复方案** | 抽取 `CelestialBodyRenderer` 抽象基类：<br>• 抽象方法：`getPosition()`, `getRadius()`, `getColors()`, `hasAtmosphere()`<br>• 共用方法：`renderWireSphere()`, `renderEquatorLine()`, `renderLODFar/Mid/Near()`<br>• `EarthRenderer` 和 `MoonRenderer` 只保留参数差异 |
| **关联架构问题** | R7（架构报告） |

---

### P1-2: 物理参数硬编码 — `Config.java` 完全未使用

| 属性 | 内容 |
|:---|:---|
| **位置** | `entity/ShipEntity.java` L20-24 + `Config.java`（未在本次扫描中） |
| **问题描述** | `ShipEntity` 使用硬编码常量：`FORCE_MAIN=0.5f`, `FORCE_VERTICAL=0.3f`, `DAMPING=0.015f`, `MAX_SPEED_TICK=25.0f`, `YAW_SPEED=2.5f`。`Config.java` 已定义同名配置项，但代码完全未读取。 |
| **风险** | 配置系统成为摆设；第二阶段不同行星需要不同重力/速度参数时无法配置化。 |
| **修复方案** | `ShipEntity` 在构造时从 `Config` 读取参数，运行时可通过配置热重载。 |
| **关联架构问题** | R5（架构报告） |

---

### P1-3: `ShipInputHandler` 发射高度 300.0 硬编码

| 属性 | 内容 |
|:---|:---|
| **位置** | `client/ShipInputHandler.java` L78 |
| **问题描述** | 主世界 `y >= 300.0` 触发自动发射到太空。300 是魔法数字，无文档说明为何是这个值。 |
| **风险** | 如果世界高度限制变化（如数据包修改），此硬编码可能不合理。 |
| **修复方案** | 抽取为 `LAUNCH_ALTITUDE` 常量，或从 `Config` 读取。 |

---

### P1-4: `ShipInputHandler` 下降键绑定风险

| 属性 | 内容 |
|:---|:---|
| **位置** | `client/ShipInputHandler.java` L92 |
| **问题描述** | 下降键使用 `mc.options.keySprint`（LeftCtrl）。虽然注释标记"冻结"，但如果其他 Mod 也监听 sprint 键或玩家改键绑定，会产生冲突。 |
| **风险** | 键绑定冲突，下降功能间歇性失效。 |
| **修复方案** | 第二阶段使用 Forge 的 `KeyMapping` 注册独立键位，避免与原版功能抢键。 |

---

### P1-5: `ShipInputHandler` 月球维度无 LaunchToSpace 触发

| 属性 | 内容 |
|:---|:---|
| **位置** | `client/ShipInputHandler.java` L78 |
| **问题描述** | 只检测 `Level.OVERWORLD` 维度 `y >= 300` 触发发射。月球维度也需要 `y >= 300` 触发返回太空（5月12日 devlog 明日计划第2项）。当前缺失。 |
| **风险** | 月球无法返回太空，返航闭环断裂。 |
| **修复方案** | 添加月球维度检测分支：若 `mc.level.dimension().equals(ModDimensions.MOON)` 且 `y >= 300`，发送 `LaunchToSpacePacket`，服务端区分处理。 |

---

### P1-6: 三层防御式 FOV 修复 — 代码异味

| 属性 | 内容 |
|:---|:---|
| **位置** | `client/ShipInputHandler.java` L30-53 |
| **问题描述** | `onPlayerTick` + `onRenderTickStart` + `onFovModifier` 三重拦截 sprinting。注释承认"根因不明"，用症状治疗替代病因治疗。虽然已冻结且有效，但属于典型的"代码疤痕"。 |
| **风险** | 后续开发者看到三重拦截会困惑；若 Forge 时序改变可能失效。 |
| **修复方案** | 保留 `ComputeFovModifierEvent` 方案（根治），删除前两重 `setSprinting(false)` 防御。添加注释说明为何 `ComputeFovModifierEvent` 足够。 |

---

### P1-7: 乘客偏移 0.8 多文件重复定义

| 属性 | 内容 |
|:---|:---|
| **位置** | `entity/ShipEntity.java` L181 + `client/ShipInputHandler.java` L138 |
| **问题描述** | 乘客 Y 轴偏移量 `0.8` 在两个文件中各自硬编码。若需调整骑乘视角高度，需改两处且容易不一致。 |
| **修复方案** | 抽取为 `ShipEntity` 的 `public static final double PASSENGER_OFFSET_Y = 0.8`，`ShipInputHandler` 引用该常量。 |

---

### P1-8: `System.out.println` 生产环境日志（4处）

| 位置 | 内容 | 建议 |
|:---|:---|:---|
| `EarthRenderer.java:82` | `[CosmicVoyage][Earth] Approaching Earth!` | 替换为 `CosmicVoyage.LOGGER.debug()` |
| `MoonRenderer.java:67` | `[CosmicVoyage][Moon] Approaching Moon!` | 替换为 `CosmicVoyage.LOGGER.debug()` |
| `ModDimensionEffects.java:47` | `[CosmicVoyage] Dimension effects registered` | 替换为 `CosmicVoyage.LOGGER.info()` |
| `ModDimensionEffects.java:49` | `[CosmicVoyage] Failed to register effects` | 替换为 `CosmicVoyage.LOGGER.error()` |

> 注：P0-3 删除 `ModDimensionEffects.java` 后，后两处自动消除。

---

### P1-9: 碰撞后速度衰减系数 0.3 无命名

| 属性 | 内容 |
|:---|:---|
| **位置** | `entity/ShipEntity.java` L66 |
| **问题描述** | `shipVelocity = shipVelocity.scale(0.3)` — 0.3 是碰撞反弹系数，无命名常量。 |
| **修复方案** | 抽取为 `COLLISION_DAMPING = 0.3f`，附带注释说明物理含义。 |

---

### P1-10: 万向节锁退化阈值 0.0001 无命名

| 属性 | 内容 |
|:---|:---|
| **位置** | `entity/ShipEntity.java` L105, L113 |
| **问题描述** | `right.lengthSqr() < 0.0001` 和 `localUp.lengthSqr() < 0.0001` — 退化判定阈值无命名。 |
| **修复方案** | 抽取为 `GIMBAL_LOCK_THRESHOLD_SQ = 0.0001f`，与 Issue #11 注释关联。 |

---

### P1-11: `handleMoonTransition` 硬编码月球传送坐标

| 属性 | 内容 |
|:---|:---|
| **位置** | `network/CosmicVoyagePacketHandler.java` L208, L215 |
| **问题描述** | 月球传送目标 `(0.0, 200.0, 0.0)` 硬编码。无注释说明为何是 200 高度，若月球地形生成变化可能玩家卡在地底。 |
| **修复方案** | 抽取常量 `MOON_ENTRY_Y = 200.0`，或根据月球地表高度动态计算。 |

---

## 四、P2 级异味（风格 / 优化）

> 可分期处理，不影响功能或稳定性。

---

### P2-1: 硬编码字符串前缀 `"[CosmicVoyage]"`

| 位置 | 建议 |
|:---|:---|
| 多处 `System.out.println` | 抽取为 `MOD_PREFIX = "[CosmicVoyage]"` 或统一使用 `LOGGER` |

---

### P2-2: `NavigationHUDRenderer`（未在本次扫描中）使用 `float[]` 存储星星

| 属性 | 内容 |
|:---|:---|
| **位置** | `client/SpaceSkyRenderer.java`（架构报告 R13） |
| **问题描述** | 使用 `float[]` 而非结构化数据（`record Star(float x, float y, float z, float size, float brightness)`），可读性差。 |
| **修复方案** | 定义 `record Star` 替换 `float[]`。 |

---

### P2-3: `ShipInputHandler` 鼠标状态未在 GUI 打开时暂停

| 属性 | 内容 |
|:---|:---|
| **位置** | `client/ShipInputHandler.java` L72 |
| **问题描述** | 打开物品栏等 GUI 时，`clientTickPhysics()` 仍执行，视角可能异常漂移（Issue #12，5月12日日志）。 |
| **修复方案** | 添加 `if (mc.screen != null) return;` 跳过物理更新。 |

---

### P2-4: `Tesselator` 局部实例高频创建

| 属性 | 内容 |
|:---|:---|
| **位置** | `EarthRenderer.java` + `MoonRenderer.java` 各渲染方法 |
| **问题描述** | 每帧每个 LOD 方法都 `Tesselator.getInstance()` + `tess.getBuilder()`，虽无实际泄漏，但可优化为复用。 |
| **修复方案** | 抽取基类后，Tesselator 实例在基类中统一获取，子方法传入 BufferBuilder 引用。 |

---

### P2-5: `onPassengerTurned` 空实现无注释说明

| 属性 | 内容 |
|:---|:---|
| **位置** | `entity/ShipEntity.java` L175-177 |
| **问题描述** | 空实现已标记"冻结"，但无 Javadoc 解释为何空实现安全。原版此方法同步乘客视角到载具，`ShipInputHandler` 已手动同步，所以空实现是设计决策而非遗漏。 |
| **修复方案** | 添加 Javadoc：`// 禁用原版视角同步 — ShipInputHandler 每 tick 手动同步 mc.player 视角到飞船` |

---

### P2-6: `ShipEntity` 后退/平移力量系数 0.5 内联

| 属性 | 内容 |
|:---|:---|
| **位置** | `entity/ShipEntity.java` L122-124 |
| **问题描述** | `back`, `strafeLeft`, `strafeRight` 的力量系数 `0.5` 内联在表达式中，无命名常量。 |
| **修复方案** | 抽取 `STRAFE_FORCE_RATIO = 0.5f`，或从 Config 读取。 |

---

## 五、重复代码精确度量

### EarthRenderer vs MoonRenderer 方法级对比

| 方法 | EarthRenderer 行号 | MoonRenderer 行号 | 行数 | 重复度 |
|:---|:---:|:---:|:---:|:---:|
| `renderWireSphere()` | 224-281 | 174-230 | 57 / 56 | **99%** |
| `renderEquatorLine()` | 285-310 | 233-257 | 26 / 25 | **95%** |
| `renderLODFar()` | 123-167 | 100-129 | 45 / 30 | **~75%**（ Billboard 大小差异） |
| `renderLODMid()` | 171-177 | 132-135 | 7 / 4 | **80%** |
| `renderLODNear()` | 181-192 | 138-149 | 12 / 12 | **90%**（大气 vs 陨石坑差异） |
| `renderInsideIndicator()` | 196-220 | 152-171 | 25 / 20 | **90%** |
| `onRenderLevelStage()` | 63-119 | 48-97 | 57 / 50 | **~85%**（事件入口骨架） |
| **合计重复行** | **229** | **198** | | |
| **占各自文件比例** | **74%** | **77%** | | |

**结论**：两个文件的核心差异仅限于**颜色数组、半径常量、位置常量、有无大气**。渲染管线 100% 相同。抽取基类后各自文件可压缩到 30-50 行。

---

## 六、跨层调用标记（违反分层架构）

| 调用方（上层） | 被调用方（下层） | 位置 | 架构问题 |
|:---|:---|:---|:---|
| `network.CosmicVoyagePacketHandler` | `entity.ShipEntity` | L6 import, L71-80, L162-184 | D1: network 依赖 entity |
| `network.CosmicVoyagePacketHandler` | `data.SpaceData` | L4 import, L249-251 | D2: network 直接操作持久化 |
| `network.CosmicVoyagePacketHandler` | `space.SpaceState` | L7 import, L287 | D2: network 依赖状态层 |
| `client.ShipInputHandler` | `entity.ShipEntity` | L4 import, L72-157 | 正常: client 允许依赖 entity |
| `command.CvSpaceCommand` | `data.SpaceData` | 架构报告 | D2: command 直接操作持久化 |

> 注：严格分层中 network 层应只传输 DTO，由 entity 层或专门同步管理器处理赋值。但 `ShipEntity` 是核心领域对象，此耦合在 Mod 规模下可接受。**P0-1 修复后风险消除**。

---

## 七、修复优先级路线图

### 第二阶段开始前必须完成（P0）

```
[P0-3] 删除 ModDimensionEffects.java ──────────────────── 0.5h
[P0-4] 一并消除 ───────────────────────────────────────── 0h
[P0-2] SpaceData 扩展双锚点 ───────────────────────────── 1h
[P0-1] ShipEntity 字段封装 + applySyncPacket() ────────── 2h
[P0-5] PacketHandler 拆分为独立文件 ───────────────────── 2h
[P0-6] LaunchToSpace 坐标 SpaceConstants 推导 ─────────── 0.5h
[P0-7] SpaceState 添加 PlayerLoggedOutEvent 清理 ──────── 0.5h
                                              总计: ~6.5h
```

### 第二阶段初期建议（P1）

```
[P1-1] 抽取 CelestialBodyRenderer 基类 ───────────────── 3h
[P1-2] Config 系统接入 ShipEntity ────────────────────── 1.5h
[P1-8] System.out -> LOGGER ───────────────────────────── 0.5h
[P1-6] 清理 FOV 防御层（保留事件方案）─────────────────── 0.5h
[P1-3/5] 发射高度常量化 + 月球触发 ────────────────────── 0.5h
[P1-7] 乘客偏移常量统一 ───────────────────────────────── 0.5h
[P1-9/10] 碰撞衰减 + 万向节锁阈值命名 ─────────────────── 0.5h
[P1-11] 月球传送坐标常量化 ────────────────────────────── 0.3h
                                              总计: ~7h
```

### 可分期（P2）

```
[P2-1] 字符串前缀常量 ────────────────────────────────── 0.3h
[P2-3] GUI 打开时暂停飞船物理 ─────────────────────────── 0.5h
[P2-5] onPassengerTurned Javadoc ──────────────────────── 0.2h
[P2-2/4/6] 其他风格项 ─────────────────────────────────── 1h
                                              总计: ~2h
```

---

## 八、与 Day 1 架构报告的关联

| 本报告编号 | 对应架构报告编号 | 状态 |
|:---|:---:|:---|
| P0-1 | R3 | network 直接操作 Entity 字段 — 新发现 17 处调用 |
| P0-2 | R2 | 单锚点 — 确认未修复 |
| P0-3 + P0-4 | R1 | 双注册 + 全吞 catch — 确认，合并处理 |
| P0-5 | R6 | 三合一文件 — 确认，新增拆分方案 |
| P0-6 | R4 | 硬编码坐标 — 确认，历史事故已记录 |
| P0-7 | R9 | 内存泄漏 — 确认，新增退出清理方案 |
| P1-1 | R7 | 重复代码 — 精确度量 74-77% |
| P1-2 | R5 | Config 未生效 — 确认 |
| P1-6 | — | 三层防御异味 — 新发现 |
| P1-7 | — | 偏移量重复 — 新发现 |

---

*报告生成: 2026-05-16*  
*Day 2 代码异味大扫除完成*
